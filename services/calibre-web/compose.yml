services:
  calibre-web-automated:
    image: crocodilestick/calibre-web-automated:latest
    container_name: calibre-web-automated
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Phoenix
      # true: skip the recursive `chown -R abc:abc /calibre-library` on every
      # ingest/start. Required under rootless-podman userns — namespace-root
      # can't chown the subuid-mapped (100999) bind mount, so the chown failed
      # EPERM per file and spammed the watchdog log (651MB on 2026-06-11).
      # Files are already correctly owned, so the chown was pointless anyway.
      - NETWORK_SHARE_MODE=true
      - TRUSTED_PROXY_COUNT=1
      # HARDCOVER_TOKEN configured in Calibre-Web general settings (not compose)
    volumes:
      - /home/almty1/calibre-config:/config
      - /mnt/calibre/ingest:/cwa-book-ingest
      - /mnt/calibre/library:/calibre-library
      - /home/almty1/calibre-metadata/metadata.db:/calibre-library/metadata.db
      # Durable disable of the CWA auto-ingest watcher. Jeff wants auto-ingest
      # permanently OFF — it races manual imports and creates phantom library
      # records (calibre-library-recovery-20260623 root cause #4). `s6-svc -d`
      # only lives in /run and was silently lost on every recreate, so the run
      # script itself is replaced with a no-op stub that marks the service down.
      # To re-enable auto-ingest: comment out this line and recreate.
      - /home/almty1/azlab/services/calibre-web/s6-overrides/cwa-ingest-service.run:/etc/s6-overlay/s6-rc.d/cwa-ingest-service/run:ro
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.calibre.rule=Host(`calibre.az-lab.dev`)"
      - "traefik.http.routers.calibre.entrypoints=websecure"
      - "traefik.http.routers.calibre.tls=true"
      - "traefik.http.routers.calibre.tls.certresolver=le"
      - "traefik.http.routers.calibre.middlewares=lan-allow@file"
      - "traefik.http.services.calibre.loadbalancer.server.port=8083"

networks:
  proxy:
    external: true
