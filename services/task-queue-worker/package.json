{
  "name": "az-task-queue-worker",
  "version": "0.1.0",
  "private": true,
  "description": "az-lab task_queue validating ingest + Cloudflare Queue consumer Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir dist",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.108.0"
  }
}
