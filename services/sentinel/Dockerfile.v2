FROM node:20-alpine
WORKDIR /app
ENV NODE_ENV=production
RUN addgroup --system --gid 1001 nodejs && adduser --system --uid 1001 sentinel
COPY package*.json ./
RUN npm ci --production && npm cache clean --force
COPY dist ./dist
USER sentinel
EXPOSE 3200
ENV PORT=3200
CMD ["node", "dist/index.js"]
